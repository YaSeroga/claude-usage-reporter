{
  "name": "claude-usage-reporter",
  "version": "0.2.1",
  "description": "Claude Code plugin/hook that reports per-session token usage and plan rate-limit windows to a claude-usage-dashboard server",
  "repository": "github:YaSeroga/claude-usage-reporter",
  "bin": { "claude-usage-reporter": "hook.js" },
  "engines": { "node": ">=18" },
  "license": "MIT",
  "private": true
}
